import { useEffect, useRef, useState } from 'react';
import { useToast } from './Toast';

type Mode = 'normal' | 'network' | 'broker' | 'worker' | 'timeout';
type DemoAction = 'submit' | 'priority' | 'scheduled' | 'failure' | 'stress' | 'complete';
type Phase = 'idle' | 'running' | 'paused' | 'success' | 'error';
type Log = { id: number; time: string; message: string; tone: 'info' | 'success' | 'error' };
type Task = { Status?: string; WorkerID?: string; Queue?: string; Priority?: number; CreatedAt?: string };
type Result = { task_ids?: string[]; task?: Task; execution_ms?: number; latency_ms?: number; message?: string };
type Output = { task_id: string; status: string; queue: string; priority: number; worker_id: string; task_type: string; created_at: string; execution_ms: number; latency_ms: number };

const code = ['// main.go — this code lives in YOUR Go application', 'package main', '', 'import (', '    "context"', '    "encoding/json"', '    "log"', '', '    "github.com/ManogyaDahal/DistQ/pkg/client"', ')', '', 'func main() {', '    ctx := context.Background()', '    sdk := client.New("http://localhost:8080")', '', '    payload, err := json.Marshal(map[string]string{', '        "message": "Hello from my Go application",', '    })', '    if err != nil { log.Fatal(err) }', '', '    response, err := sdk.SubmitTask(ctx, client.SubmitTaskRequest{', '        Type: "demo.sleep",', '        Payload: payload,', '        Priority: 5,', '        Source: "my-go-service",', '    })', '    if err != nil { log.Fatal("DistQ task submission:", err) }', '', '    log.Printf("Task %s is %s", response.ID, response.Status)', '}'];
const modeCode: Record<Mode, string[]> = {
  normal: code,
  network: [...code, '', '// Handle typed HTTP/network errors returned by SubmitTask.'],
  broker: [...code, '', '// Handle a typed broker/API error returned by SubmitTask.'],
  worker: [...code, '', '// Inspect the typed response or error returned by SubmitTask.'],
  timeout: ['// Set your own context deadline in the calling application.', ...code],
};
const stages = ['Your Go Application', 'pkg/client SDK', 'HTTP POST /api/task', 'DistQ API Server', 'Broker', 'Redis Streams', 'Worker', 'Task Completed'];
const labels: Record<Mode, string> = { normal: 'Normal', network: 'Network Failure', broker: 'Broker Offline', worker: 'Worker Failure', timeout: 'Timeout' };
const demoLabels: Record<DemoAction, string> = { submit: 'Submit Demo Task', priority: 'Priority Demo', scheduled: 'Scheduled Demo', failure: 'Failure Demo', stress: 'Stress Test', complete: 'Complete Demo' };
const now = () => new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: false });
const ready = (): Log => ({ id: Date.now(), time: now(), message: 'Ready to demonstrate how your Go application communicates with DistQ.', tone: 'info' });

export default function SDKPlayground({ onViewDashboard }: { onViewDashboard?: (taskId: string) => void }) {
  const { showToast } = useToast();
  const [phase, setPhase] = useState<Phase>('idle');
  const [mode, setMode] = useState<Mode>('normal');
  const [demoAction, setDemoAction] = useState<DemoAction>('submit');
  const [logs, setLogs] = useState<Log[]>(() => [ready()]);
  const [line, setLine] = useState(-1);
  const [stage, setStage] = useState(-1);
  const [request, setRequest] = useState<object | null>(null);
  const [response, setResponse] = useState<Output | null>(null);
  const [elapsed, setElapsed] = useState<number | null>(null);
  const [showProtocol, setShowProtocol] = useState(false);
  const logRef = useRef<HTMLDivElement>(null);
  const paused = useRef(false);
  const token = useRef(0);
  const controller = useRef<AbortController | null>(null);
  const timers = useRef<number[]>([]);
  const liveCode = [...modeCode[mode].map(value => {
    if (demoAction === 'priority' && value.includes('Priority: 5')) return '        Priority: 10, // high-priority work';
    if (demoAction === 'failure' && value.includes('Type: "demo.sleep"')) return '        Type: "demo.fail", // demonstrate retry/error handling';
    if (demoAction === 'scheduled' && value.includes('Priority: 5')) return '        Priority: 5, // add ETA: &runAt for scheduled work';
    return value;
  }), '', `// Active playground demo: ${demoLabels[demoAction]}`];
  const sdkLine = liveCode.findIndex(value => value.startsWith('sdk :='));
  const taskLine = liveCode.findIndex(value => value.trimStart().startsWith('payload, err :='));
  const payloadLine = liveCode.findIndex(value => value.trimStart().startsWith('Payload:'));
  const enqueueLine = liveCode.findIndex(value => value.trimStart().startsWith('response, err := sdk.SubmitTask'));

  const cancel = () => { token.current += 1; controller.current?.abort(); controller.current = null; timers.current.forEach(clearTimeout); timers.current = []; paused.current = false; };
  const reset = () => { cancel(); setPhase('idle'); setLogs([ready()]); setLine(-1); setStage(-1); setRequest(null); setResponse(null); setElapsed(null); };
  useEffect(() => { logRef.current?.scrollTo({ top: logRef.current.scrollHeight, behavior: 'smooth' }); }, [logs]);
  useEffect(() => () => { cancel(); }, []);
  const add = (message: string, tone: Log['tone'] = 'info') => setLogs(items => [...items, { id: Date.now() + Math.random(), time: now(), message, tone }]);
  const copy = async (value: string, message: string) => { try { await navigator.clipboard.writeText(value); showToast('✓ ' + message, 'success'); } catch { showToast('Unable to copy to clipboard.', 'error'); } };
  const sleep = (ms: number, id: number) => new Promise<boolean>(resolve => {
    const check = () => { if (id !== token.current) return resolve(false); if (paused.current) { timers.current.push(window.setTimeout(check, 80)); return; } timers.current.push(window.setTimeout(() => resolve(id === token.current), ms)); };
    check();
  });
  const step = async (message: string, codeLine: number, arch: number, id: number, tone: Log['tone'] = 'info') => {
    if (!await sleep(480, id)) return false;
    setLine(codeLine); setStage(arch); add(message, tone); return true;
  };
  const finishError = (message: string, recovery: string, at: number, started: number) => { setLine(at); add(message, 'error'); add(recovery); setResponse(null); setElapsed(Math.round(performance.now() - started)); setPhase('error'); };

  const run = async (action = demoAction) => {
    setDemoAction(action);
    const id = ++token.current; const started = performance.now(); controller.current = new AbortController(); paused.current = false;
    setPhase('running'); setLogs([]); setLine(-1); setStage(-1); setRequest(null); setResponse(null); setElapsed(null);
    const payload = { type: action === 'failure' ? 'demo.fail' : 'demo.sleep', payload: { message: 'Hello from my Go application' }, priority: action === 'priority' ? 10 : 5, source: 'my-go-service' };
    try {
      if (!await step('Initializing Go Client SDK with client.New(...).', sdkLine, 0, id)) return;
      if (mode === 'network') return finishError('SDK request failed: dial tcp localhost:8080: network is unreachable.', 'Recovery: check the API endpoint and retry.', sdkLine, started);
      if (!await step('Preparing task payload in your Go application.', taskLine, 1, id)) return;
      if (!await step('Calling sdk.SubmitTask().', enqueueLine, 1, id, 'success')) return;
      if (!await step('SDK serializes the payload into JSON.', payloadLine, 1, id)) return;
      setRequest(payload);
      if (!await step('SDK sends POST /api/task to DistQ.', enqueueLine, 2, id)) return;
      if (mode === 'timeout') return finishError('context deadline exceeded while waiting for POST /tasks.', 'Recovery: SDK cancelled the request safely; increase the timeout and retry.', enqueueLine, started);
      if (mode === 'broker') return finishError('SDK enqueue failed: broker is unavailable (connection refused).', 'Recovery: reconnect the broker, then replay this demonstration.', enqueueLine, started);
      const api = await fetch(`/api/sdk/${action}`, { method: 'POST', signal: controller.current.signal });
      const data: Result & { error?: string; details?: string } = await api.json();
      if (!api.ok) throw new Error(data.details || data.error || 'SDK request was rejected.');
      if (!await step('API validates the request.', enqueueLine, 3, id, 'success')) return;
      if (!await step('Broker receives the task.', enqueueLine, 4, id, 'success')) return;
      if (!await step('Redis Streams stores the task.', enqueueLine, 5, id, 'success')) return;
      if (!await step('Worker claims the task.', enqueueLine, 6, id, 'success')) return;
      if (mode === 'worker') return finishError('Worker returned an execution error: demo.print handler failed.', 'Recovery: task marked for retry; inspect worker logs, then replay.', enqueueLine, started);
      if (!await step('Worker executes the task.', enqueueLine, 7, id, 'success')) return;
      const task = data.task || {};
      // latency_ms is returned by the API and is kept as the single source of truth for both views.
      const output: Output = { task_id: data.task_ids?.[0] || 'pending', status: 'completed', queue: task.Queue || 'default', priority: task.Priority || 5, worker_id: task.WorkerID || 'homie-44841', task_type: 'demo.print', created_at: task.CreatedAt || new Date().toISOString(), execution_ms: 312, latency_ms: data.latency_ms ?? data.execution_ms ?? 0 };
      setResponse(output);
      if (!await step('SDK polls task status and receives a typed completed response.', enqueueLine, 7, id, 'success')) return;
      add('SDK demo completed successfully.', 'success'); setElapsed(Math.round(performance.now() - started)); setPhase('success');
    } catch (error) {
      if (id !== token.current || (error instanceof DOMException && error.name === 'AbortError')) return;
      finishError('SDK request failed: ' + (error instanceof Error ? error.message : 'Unknown error.'), 'Recovery: verify API and broker availability, then retry.', enqueueLine, started);
    } finally { if (id === token.current) controller.current = null; }
  };

  const togglePause = () => { if (phase === 'running') { paused.current = true; setPhase('paused'); add('Demo paused.'); } else if (phase === 'paused') { paused.current = false; setPhase('running'); add('Demo resumed.'); } };
  const busy = phase === 'running' || phase === 'paused';
  const formatDuration = (ms: number) => ms >= 1000 ? (ms / 1000).toFixed(1) + ' s' : ms + ' ms';
  const errorStatus = mode === 'timeout' ? '🟠 Timeout' : '🔴 Failed';

  return <div style={s.root}>
    <style>{'@keyframes sdk-in{from{opacity:0;transform:translateY(6px)}to{opacity:1}}.sdk-log{animation:sdk-in .25s ease both}.sdk-code-line{display:block;padding:0 12px;min-height:20px;white-space:pre;transition:background .28s ease,color .28s ease,box-shadow .28s ease}.sdk-code-line.active{background:#2e2a1a;color:#fff;border-left:2px solid #c9b86c;box-shadow:inset 0 0 20px rgba(201,184,108,.08)}.sdk-s{color:#a8d7a8}.sdk-k{color:#d5b3ee}.sdk-f{color:#8fc6e8}.sdk-c{color:#747474}.sdk-v{color:#e8c785}'}</style>
    <div style={s.hero}><div><div style={s.eyebrow}>Public Go SDK</div><h2 style={s.title}>Go Client SDK Demonstration</h2><p style={s.muted}>Demonstrates how a third-party Go application integrates with DistQ using the public <code>pkg/client</code> SDK. Developers write simple Go code while the SDK automatically handles HTTP communication, request serialization, response parsing, and typed Go models.</p></div><span style={statusBadge(phase, mode === 'timeout')}>{phase === 'success' ? '🟢 COMPLETED' : phase === 'error' ? (mode === 'timeout' ? '🟠 TIMEOUT' : '🔴 FAILED') : phase === 'paused' ? 'Ⅱ PAUSED' : busy ? '🟡 RUNNING' : '● READY'}</span></div>
    <div style={s.demoActions}>{(Object.keys(demoLabels) as DemoAction[]).map(action => <button key={action} disabled={busy} onClick={() => run(action)} style={{ ...s.demoButton, ...(demoAction === action ? s.demoActive : {}) }}>{demoLabels[action]}</button>)}</div>
    <div style={s.controls}><button disabled={busy} onClick={() => run()} style={s.primary}>▶ Run Demo</button><button disabled={!busy} onClick={togglePause} style={s.button}>{phase === 'paused' ? '▶ Resume' : 'Ⅱ Pause'}</button><button disabled={busy} onClick={() => run()} style={s.button}>↻ Replay</button><button onClick={reset} style={s.button}>↺ Reset</button><button disabled={busy} onClick={() => setLogs([])} style={s.button}>⌫ Clear Logs</button><label style={s.mode}>Demo mode <select disabled={busy} value={mode} onChange={e => setMode(e.target.value as Mode)} style={s.select}>{(Object.keys(labels) as Mode[]).map(key => <option key={key} value={key}>{labels[key]}</option>)}</select></label></div>
    <div style={s.progress}><div style={{ ...s.fill, width: (Math.max(0, stage + 1) / stages.length * 100) + '%' }} /></div>
    <div style={s.grid}><Panel title="Execution timeline"><div ref={logRef} style={s.timeline}>{logs.length ? logs.map((entry, index) => <div className="sdk-log" key={entry.id} style={s.event}><i style={{ ...s.dot, background: entry.tone === 'error' ? 'var(--color-status-danger-text)' : entry.tone === 'success' ? 'var(--color-status-ok-text)' : 'var(--color-status-warn-text)' }} /><div><time style={s.time}>[{entry.time}]</time><div style={{ color: entry.tone === 'error' ? 'var(--color-status-danger-text)' : entry.tone === 'success' ? 'var(--color-status-ok-text)' : 'var(--color-text-primary)' }}>{entry.message}</div>{index < logs.length - 1 && <div style={s.arrow}>↓</div>}</div></div>) : <div style={s.empty}>Logs cleared. Run a demo to begin.</div>}</div></Panel><Panel title="Third-party Application Code"><div style={s.codeNote}>This code exists in <b>YOUR application</b>—not inside DistQ.</div><div style={s.codeToolbar}><span>main.go</span><button style={s.copy} onClick={() => copy(liveCode.join('\n'), 'Go code copied')}>Copy code</button></div><pre style={s.code}>{liveCode.map((value, index) => <code key={index} className={'sdk-code-line ' + (line === index ? 'active' : '')}>{highlightGo(value) || ' '}</code>)}</pre></Panel></div>
    <Panel title="Behind the Scenes"><button onClick={() => setShowProtocol(value => !value)} style={s.expand}>{showProtocol ? '− Hide request flow' : '+ Show request flow'}<span>{showProtocol ? 'Collapse' : 'Expand'}</span></button>{showProtocol && <div style={s.protocolFlow}><ProtocolStep title="HTTP Request" detail="POST /api/task" body={request ? JSON.stringify(request, null, 2) : '// Request appears when the demo starts.'} /><span style={s.protocolArrow}>↓</span><ProtocolStep title="HTTP Response" detail="JSON returned by DistQ" body={response ? JSON.stringify(response, null, 2) : '// Response appears after the task completes.'} /><span style={s.protocolArrow}>↓</span><ProtocolStep title="Parsed Go Object" detail="client.SubmitTaskResponse" body={response ? `SubmitTaskResponse{\n    ID: "${response.task_id}",\n    Status: "${response.status}",\n}` : 'SubmitTaskResponse{\n    ID: "...",\n    Status: "pending",\n}'} /></div>}</Panel>
    <Panel title="Developer Workflow"><p style={s.workflowCopy}>Follow the complete request flow from your Go application into DistQ. Each node animates as the selected demo executes.</p><div style={s.arch}>{stages.map((name, index) => <div key={name} style={{ display: 'contents' }}><div style={{ ...s.node, ...(index <= stage ? s.nodeOn : {}) }}>{index < stage ? '✓ ' : index === stage ? '◉ ' : '○ '}{name}</div>{index < stages.length - 1 && <span style={s.archArrow}>↓</span>}</div>)}</div></Panel>
    {(phase === 'success' || phase === 'error') && <section style={{ ...s.done, borderColor: phase === 'success' ? 'var(--color-status-ok)' : mode === 'timeout' ? 'var(--color-status-warn)' : 'var(--color-status-danger-border)' }}><b style={{ color: phase === 'success' ? 'var(--color-status-ok-text)' : mode === 'timeout' ? 'var(--color-status-warn-text)' : 'var(--color-status-danger-text)' }}>{phase === 'success' ? '✓ SDK Demo Completed Successfully' : 'SDK Demo Did Not Complete'}</b><div style={s.summaryLead}><span style={statusBadge(phase, mode === 'timeout')}>{phase === 'success' ? '🟢 Completed' : errorStatus}</span><span style={s.duration}><span>Workflow Duration</span>{formatDuration(elapsed || 0)}</span></div>{phase === 'success' && response && <><div style={s.details}><Detail label="Task ID" value={response.task_id} onCopy={() => copy(response.task_id, 'Task ID copied')} /><Detail label="Status" value="🟢 Completed" /><Detail label="Queue" value={response.queue} /><Detail label="Priority" value={response.priority} /><Detail label="Worker" value={response.worker_id} /><Detail label="Task Type" value={response.task_type} /><Detail label="Execution Time" value={response.execution_ms + ' ms'} /><Detail label="API Latency" value={response.latency_ms + ' ms'} /><Detail label="Workflow Duration" value={formatDuration(elapsed || 0)} /></div><button style={{ ...s.button, ...s.dashboardButton }} onClick={() => onViewDashboard?.(response.task_id)}>View in Dashboard →</button></>}</section>}
    <section style={s.responsibilities}><div style={s.responsibilityTitle}><div style={s.eyebrow}>One clear boundary</div><h3 style={s.autoTitle}>Developer vs SDK Responsibilities</h3></div><div style={s.responsibilityGrid}><div><b>Developer writes</b>{['sdk.SubmitTask()', 'sdk.GetTask()', 'sdk.GetMetrics()', 'sdk.GetWorkers()'].map(item => <span key={item}>• {item}</span>)}</div><div><b>SDK automatically</b>{['Serialize JSON', 'Create HTTP request', 'Send request', 'Parse response', 'Return typed Go objects'].map(item => <span key={item}>• {item}</span>)}</div></div></section>
    <section style={s.auto}><h3 style={s.autoTitle}>What the SDK Handles Automatically</h3><div style={s.autoList}>{['JSON serialization', 'HTTP communication', 'Request validation', 'Response parsing', 'Typed Go models', 'Error handling', 'Context propagation'].map(item => <span key={item}>✓ {item}</span>)}</div></section>
  </div>;
}

function highlightGo(value: string) { const matcher = /(\/\/.*|"(?:\\.|[^"\\])*"|\b(?:if|return|defer|nil)\b|\b(?:sdk|client|context|fmt|errors)\b|\b(?:NewSDK|Enqueue|Task|WithTimeout|DeadlineExceeded|Errorf|New)\b|\b(?:Type|Payload|Priority)\b)/g; const parts = value.split(matcher); return <>{parts.map((part, index) => <span key={index} className={part.startsWith('//') ? 'sdk-c' : part.startsWith('"') ? 'sdk-s' : /^(if|return|defer|nil)$/.test(part) ? 'sdk-k' : /^(NewSDK|Enqueue|Task|WithTimeout|DeadlineExceeded|Errorf|New)$/.test(part) ? 'sdk-f' : /^(Type|Payload|Priority)$/.test(part) ? 'sdk-v' : ''}>{part}</span>)}</>; }
function Panel({ title, children }: { title: string; children: React.ReactNode }) { return <section style={s.panel}><header style={s.panelHead}>{title}</header>{children}</section>; }
function ProtocolStep({ title, detail, body }: { title: string; detail: string; body: string }) { return <div style={s.protocolStep}><div style={s.protocolHead}><b>{title}</b><span>{detail}</span></div><pre style={s.protocolCode}>{body}</pre></div>; }
function Detail({ label, value, onCopy }: { label: string; value: string | number; onCopy?: () => void }) { return <div><div style={s.detailLabel}>{label}</div><div style={s.detailValue}>{value}{onCopy && <button style={{ ...s.copy, marginLeft: '7px' }} onClick={onCopy}>Copy</button>}</div></div>; }
function statusBadge(phase: Phase, timeout = false) { const color = phase === 'success' ? 'var(--color-status-ok-text)' : phase === 'error' ? (timeout ? 'var(--color-status-warn-text)' : 'var(--color-status-danger-text)') : phase === 'running' ? 'var(--color-status-warn-text)' : 'var(--color-text-muted)'; const bg = phase === 'success' ? 'var(--color-status-ok-bg)' : phase === 'error' ? (timeout ? 'var(--color-status-warn-bg)' : 'var(--color-status-danger-bg)') : phase === 'running' ? 'var(--color-status-warn-bg)' : 'var(--color-bg-elevated)'; return { color, background: bg, fontFamily: 'var(--font-mono)', fontSize: '10px', padding: '4px 8px', borderRadius: '999px' }; }

const s = { copy: { background: 'transparent', border: '1px solid var(--color-border-hover)', color: 'var(--color-text-secondary)', borderRadius: 'var(--radius-sm)', padding: '3px 7px', fontSize: '10px', cursor: 'pointer' }, root: { display: 'flex', flexDirection: 'column', gap: '18px', maxWidth: '1100px', margin: '0 auto' }, hero: { display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', gap: '24px', flexWrap: 'wrap', padding: '8px 0 4px' }, eyebrow: { color: 'var(--color-status-warn-text)', fontSize: '10px', fontWeight: 700, fontFamily: 'var(--font-mono)', textTransform: 'uppercase', letterSpacing: '.11em' }, title: { margin: '4px 0 8px', color: 'var(--color-text-emphasis)', fontSize: '30px', lineHeight: 1.15, letterSpacing: '-.04em' }, muted: { color: 'var(--color-text-secondary)', fontSize: '13px', lineHeight: 1.7, maxWidth: '760px' }, demoActions: { display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(145px, 1fr))', gap: '8px' }, demoButton: { padding: '9px 10px', background: 'var(--color-bg-input)', border: '1px solid var(--color-border-default)', borderRadius: 'var(--radius-sm)', color: 'var(--color-text-secondary)', cursor: 'pointer', fontSize: '11px' }, demoActive: { color: 'var(--color-text-emphasis)', borderColor: 'var(--color-status-warn)', background: 'var(--color-status-warn-bg)' }, controls: { display: 'flex', gap: '8px', flexWrap: 'wrap', alignItems: 'center' }, button: { background: 'var(--color-bg-elevated)', border: '1px solid var(--color-border-hover)', color: 'var(--color-text-primary)', borderRadius: 'var(--radius-sm)', padding: '7px 10px', cursor: 'pointer', fontSize: '12px' }, primary: { background: 'var(--color-status-warn-bg)', border: '1px solid var(--color-status-warn)', color: 'var(--color-text-emphasis)', borderRadius: 'var(--radius-sm)', padding: '7px 10px', cursor: 'pointer', fontSize: '12px' }, mode: { color: 'var(--color-text-secondary)', display: 'flex', gap: '7px', alignItems: 'center', fontSize: '12px', marginLeft: '4px' }, select: { background: 'var(--color-bg-elevated)', border: '1px solid var(--color-border-hover)', color: 'var(--color-text-primary)', borderRadius: 'var(--radius-sm)', padding: '6px 8px' }, progress: { height: '3px', borderRadius: '3px', background: 'var(--color-bg-elevated)', overflow: 'hidden' }, fill: { height: '100%', background: 'var(--color-status-warn-text)', transition: 'width .35s ease' }, grid: { display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(310px, 1fr))', gap: '16px' }, panel: { background: 'var(--color-bg-surface)', border: '1px solid var(--color-border-default)', borderRadius: 'var(--radius-md)', overflow: 'hidden' }, panelHead: { padding: '11px 14px', borderBottom: '1px solid var(--color-border-default)', color: 'var(--color-text-secondary)', fontSize: '10px', fontWeight: 600, letterSpacing: '.08em', textTransform: 'uppercase' }, timeline: { height: '292px', overflowY: 'auto', padding: '15px', background: 'var(--color-bg-input)', fontFamily: 'var(--font-mono)', fontSize: '12px' }, event: { display: 'grid', gridTemplateColumns: '13px 1fr', gap: '8px', minHeight: '38px' }, dot: { width: '7px', height: '7px', borderRadius: '50%', marginTop: '6px' }, time: { color: 'var(--color-text-muted)', fontSize: '10px' }, arrow: { color: 'var(--color-text-faint)', height: '16px' }, empty: { color: 'var(--color-text-muted)', paddingTop: '110px', textAlign: 'center' }, codeNote: { padding: '9px 12px', color: 'var(--color-status-warn-text)', background: 'var(--color-status-warn-bg)', borderBottom: '1px solid var(--color-status-warn)', fontSize: '11px' }, codeToolbar: { padding: '7px 12px', borderBottom: '1px solid var(--color-border-default)', display: 'flex', justifyContent: 'space-between', color: 'var(--color-text-muted)', fontFamily: 'var(--font-mono)', fontSize: '10px' }, code: { margin: 0, height: '292px', overflow: 'auto', padding: '15px 0', background: '#090909', color: '#d2d2d2', fontSize: '12px', lineHeight: 1.65, fontFamily: 'var(--font-mono)' }, arch: { padding: '16px', display: 'flex', gap: '8px', alignItems: 'center', justifyContent: 'space-between', flexWrap: 'wrap' }, node: { padding: '9px 11px', border: '1px solid var(--color-border-default)', borderRadius: 'var(--radius-sm)', color: 'var(--color-text-muted)', fontFamily: 'var(--font-mono)', fontSize: '11px', transition: 'all .25s ease' }, nodeOn: { borderColor: 'var(--color-status-ok)', background: 'var(--color-status-ok-bg)', color: 'var(--color-status-ok-text)', animation: 'sdk-glow 1.2s ease-in-out' }, archArrow: { color: 'var(--color-text-faint)' }, workflowCopy: { padding: '12px 16px 0', color: 'var(--color-text-secondary)', fontSize: '12px' }, expand: { width: '100%', border: 0, background: 'var(--color-bg-input)', color: 'var(--color-text-primary)', textAlign: 'left', padding: '12px 14px', cursor: 'pointer', display: 'flex', justifyContent: 'space-between' }, protocolFlow: { padding: '15px', display: 'grid', gap: '8px', borderTop: '1px solid var(--color-border-default)' }, protocolStep: { border: '1px solid var(--color-border-default)', borderRadius: 'var(--radius-sm)', overflow: 'hidden', background: 'var(--color-bg-input)' }, protocolHead: { display: 'flex', justifyContent: 'space-between', gap: '12px', padding: '8px 10px', borderBottom: '1px solid var(--color-border-default)', color: 'var(--color-text-secondary)', fontSize: '11px' }, protocolCode: { margin: 0, padding: '10px', color: '#b8d6ef', fontFamily: 'var(--font-mono)', fontSize: '11px', lineHeight: 1.55, overflow: 'auto' }, protocolArrow: { color: 'var(--color-status-warn-text)', textAlign: 'center' }, done: { padding: '15px', background: 'var(--color-bg-surface)', border: '1px solid', borderRadius: 'var(--radius-md)', display: 'grid', gap: '10px' }, summaryLead: { display: 'flex', alignItems: 'center', gap: '12px', flexWrap: 'wrap' }, duration: { display: 'flex', gap: '6px', color: 'var(--color-text-primary)', fontFamily: 'var(--font-mono)', fontSize: '12px' }, details: { marginTop: '3px', paddingTop: '12px', borderTop: '1px solid var(--color-border-default)', display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(120px, 1fr))', gap: '12px' }, detailLabel: { color: 'var(--color-text-muted)', fontSize: '10px', textTransform: 'uppercase', letterSpacing: '.06em' }, detailValue: { color: 'var(--color-text-primary)', fontFamily: 'var(--font-mono)', fontSize: '11px', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }, dashboardButton: { justifySelf: 'start', marginTop: '2px', borderColor: 'var(--color-status-ok)', color: 'var(--color-status-ok-text)' }, responsibilities: { padding: '18px', display: 'grid', gridTemplateColumns: 'minmax(220px, .8fr) 1.2fr', gap: '24px', background: 'var(--color-bg-input)', border: '1px solid var(--color-border-default)', borderRadius: 'var(--radius-md)' }, responsibilityTitle: { alignSelf: 'center' }, responsibilityGrid: { display: 'grid', gridTemplateColumns: 'repeat(2, minmax(0, 1fr))', gap: '12px' }, auto: { padding: '20px', background: 'var(--color-bg-input)', border: '1px solid var(--color-border-default)', borderRadius: 'var(--radius-md)' }, autoTitle: { color: 'var(--color-text-emphasis)', fontSize: '18px', margin: '4px 0 14px' }, autoList: { display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(160px, 1fr))', gap: '9px', color: 'var(--color-status-ok-text)', fontSize: '12px' } } as const;
